<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/movie_poster_iv"
        android:layout_width="350dp"
        android:layout_height="350dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        tools:src="@drawable/demo_img" />
    <!-- <TextView
        android:id="@+id/movie_title_tv"
        android:layout_width="350dp"
        android:layout_height="wrap_content"
        android:textSize="@dimen/movie_title_size"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toBottomOf="@id/movie_poster_iv"
        tools:text="Movie Title Here"
        android:padding="16dp"
        android:textStyle="bold"
        android:background="@drawable/movie_title_gradient"
        android:textColor="@color/textview_common_color"/> -->
</android.support.constraint.ConstraintLayout>
<!-- TODO(1) Correct the layout(if possible) -->
<!-- TODO(2) Correct the TextView cutting using ellipsize -->